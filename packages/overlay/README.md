# @hintora/overlay

Spotlight, tooltip and step chip, rendered inside a closed shadow root.

Vanilla TS with hand-written scoped CSS. It is injected into arbitrary host
pages, so it must not inherit their styles or leak its own. The React ban is
enforced by an ESLint rule, not by convention.

Filled in by the `overlay` phase.
