# @hintora/sdk

The second distribution target: one `hintora.js` a SaaS customer drops in with
`<script src="..." data-key="...">`.

Same core, same overlay, no extension APIs. Building both from one core proves
the architecture is not coupled to the extension surface.

Filled in by the `sdk-build` phase.
